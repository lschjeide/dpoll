<%= simple_form_for(event) do |f| %>
    <%= f.input :name, label:'Event Name', error_html: {id: 'name'} %>
    <%= f.input :description, error_html: {id: 'description'} %>
    <%= f.submit 'Add', class: 'btn btn-primary' %>
<% end %>
